%% -*- mode: erlang; -*-
%% rebar3 configuration for zotonic_mod_ambit

{erl_opts, [debug_info]}.

{deps, [
    %% TODO: replace the URL below with the actual ambit repository URL
    {ambit, {git, "https://github.com/example/ambit.git", {branch, "main"}}}
]}.

{profiles, [
    {test, [
        {erl_opts, [debug_info]},
        {deps, []}
    ]}
]}.
